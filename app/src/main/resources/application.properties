spring.application.name=app


# =========================================================
#  TaskFlow API — Application Configuration
# =========================================================

server.port=${PORT:8080}

# Enable H2 console
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console


# Test configuration — H2 in-memory database
# spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# app.jwt.secret=TestSecretKeyThatIsLongEnoughForHS512AlgorithmInTests1234567890
# app.jwt.expiration-ms=3600000
# app.jwt.refresh-expiration-ms=86400000

# app.upload.dir=test-uploads

# app.anthropic.api-key=test-key

# # ---------------------------------------------------------
# # DataSource — PostgreSQL
# # Override via environment variables in production
# # ---------------------------------------------------------
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.divizczygmewtrlakjpo}
spring.datasource.password=${DB_PASSWORD:lEL7c9iqh6MN9hHS}
spring.datasource.driver-class-name=org.postgresql.Driver


# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# # ---------------------------------------------------------
# # JPA / Hibernate
# # ---------------------------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# # ---------------------------------------------------------
# # JWT
# # ---------------------------------------------------------
app.jwt.secret=${JWT_SECRET:ThisIsAVeryLongAndSecureSecretKeyForTaskFlowApplicationThatShouldBeChangedInProduction}
# app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

# # ---------------------------------------------------------
# # File Upload
# # ---------------------------------------------------------
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB
app.upload.dir=${UPLOAD_DIR:uploads}




app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# # ---------------------------------------------------------
# # Mail (for notifications)
# # ---------------------------------------------------------
# spring.mail.host=${MAIL_HOST:smtp.gmail.com}
# spring.mail.port=${MAIL_PORT:587}
# spring.mail.username=${MAIL_USERNAME:}
# spring.mail.password=${MAIL_PASSWORD:}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# app.mail.from=${MAIL_FROM:noreply@taskflow.com}

# # ---------------------------------------------------------
# # Anthropic AI (optional task description generation)
# # ---------------------------------------------------------
# app.anthropic.api-key=${ANTHROPIC_API_KEY:}
# app.anthropic.model=claude-sonnet-4-20250514
# app.anthropic.max-tokens=512

# # ---------------------------------------------------------
# # OpenAPI / Swagger
# # ---------------------------------------------------------
# springdoc.api-docs.path=/api-docs
# springdoc.swagger-ui.path=/swagger-ui.html
# springdoc.swagger-ui.operationsSorter=method

# # ---------------------------------------------------------
# # Logging
# # ---------------------------------------------------------
logging.level.com.taskflow=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n